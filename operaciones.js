const registrar = (nombreAnimal, Edad, Tipo, color, enfermedad) => {
    
};

const leer = () => {
  //mostrar por consola las citas de animales registrados  
};

module.export = {
  registrar,
  leer
}; 